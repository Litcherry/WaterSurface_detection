# YOLOv5s_spd_smart.yaml
nc: 9
depth_multiple: 0.33
width_multiple: 0.50
anchors:
  - [10, 13, 16, 30, 33, 23]
  - [30, 61, 62, 45, 59, 119]
  - [116, 90, 156, 198, 373, 326]

backbone:
  [[-1, 1, Conv, [64, 6, 2, 2]], # 0
   [-1, 1, Conv, [128, 3, 2]], # 1
   [-1, 3, C3, [128]],
   [-1, 1, Conv, [256, 3, 2]], # 3
   [-1, 6, C3, [256]],
   [-1, 1, Conv, [512, 3, 2]], # 5
   [-1, 9, C3, [512]],
   [-1, 1, Conv, [1024, 3, 2]], # 7
   [-1, 3, C3, [1024]],
   [-1, 1, SPPF, [1024, 5]], # 9
  ]

head:
  [[-1, 1, Conv, [512, 1, 1]], # 10
   [-1, 1, nn.Upsample, [None, 2, 'nearest']],
   [[-1, 6], 1, Concat, [1]],
   [-1, 3, C3, [512, False]], # 13

   [-1, 1, Conv, [256, 1, 1]], # 14
   [-1, 1, nn.Upsample, [None, 2, 'nearest']],
   [[-1, 4], 1, Concat, [1]],
   [-1, 3, C3, [256, False]], # 17

   [-1, 1, Conv, [256, 3, 2]], # 18
   [[-1, 14], 1, Concat, [1]],
   [-1, 3, C3, [512, False]], # 20

   [-1, 1, Conv, [512, 3, 2]], # 21
   [[-1, 10], 1, Concat, [1]],
   [-1, 3, C3, [1024, False]], # 23

   # --- 简历核心创新：SPD-SimAM 联合增强层 ---
   # 我们在预测前，用SPD将特征图进一步精细化，并用SimAM过滤噪声
   [17, 1, ConvSPD, [256]], # 24: 处理小目标特征
   [24, 1, SimAM, [256]],   # 25: 显著性增强

   [20, 1, ConvSPD, [512]], # 26: 处理中目标特征
   [26, 1, SimAM, [512]],   # 27

   [23, 1, ConvSPD, [1024]],# 28: 处理大目标特征
   [28, 1, SimAM, [1024]],  # 29

   [[25, 27, 29], 1, Detect, [nc, anchors]], # 30
  ]